services:
  # Backend Service
  - type: web
    name: jo-backend
    env: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false # User must provide this in Render Dashboard
      - key: JWT_SECRET
        sync: false # User must provide this or generate one
    rootDirectory: backend

  # Frontend Service (Next.js)
  - type: web
    name: jo-frontend
    env: node
    plan: free
    region: singapore
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: jo-backend
          property: host
    rootDirectory: frontend
